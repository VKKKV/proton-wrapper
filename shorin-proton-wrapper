#!/usr/bin/env bash
set -euo pipefail

DEFAULT_PREFIX="${PROTON_PREFIX:-$HOME/.proton}"
XDG_DATA_HOME_VALUE="${XDG_DATA_HOME:-$HOME/.local/share}"
LOCAL_RUNNER_DIR="$XDG_DATA_HOME_VALUE/proton/runners"
STEAM_ROOT="${STEAM_COMPAT_CLIENT_INSTALL_PATH:-$HOME/.steam/root}"

ACTION=""
PRODUCT=""
VERSION="latest"
PREFIX="$DEFAULT_PREFIX"
RUNNER_ARG="${PROTON_RUNNER:-}"
DOWNLOAD_DIR=""
INSTALL_DIR=""
INSTALL_TO="local"
CLEAN=0
TARGET=""
TARGET_ARGS=()
LANG_OVERRIDE="${PROTON_LANG:-}"

RUNNER_TYPES=()
RUNNER_SOURCES=()
RUNNER_NAMES=()
RUNNER_PATHS=()
declare -A RUNNER_SEEN=()

ui_lang() {
  local locale="${LANG_OVERRIDE:-${LC_ALL:-${LC_MESSAGES:-${LANG:-}}}}"
  case "${locale,,}" in
    zh|zh_*|zh.*|zh-*) printf 'zh\n' ;;
    *) printf 'en\n' ;;
  esac
}

usage_en() {
  cat <<'EOF'
Usage:
  shorin-proton-wrapper [options] app.exe [args...]

Options:
  --init                  initialize prefix, default ~/.proton
  -p, --prefix DIR        prefix directory, initialize if needed
  --status                show prefix information
  --list-runner           list installed runners
  --runner PATH|ge|dw     use specified runner
  --install ge|dw         install runner to ~/.local/share/proton/runners
  --install-steam ge|dw   install runner for Steam
  --install-lutris ge|dw  install runner for Lutris
  --download ge|dw        download runner only, default XDG download directory
  --list-version ge|dw    list available runner versions
  --version VERSION       use specific release tag, default latest
  --update                update all installed Latest runners
  --remove ge|dw          remove runner from ~/.local/share/proton/runners
  --remove-steam [ge|dw]  remove runner from Steam, or all wrapper-installed runners
  --remove-lutris [ge|dw] remove runner from Lutris, or all wrapper-installed runners
  --help-full             show detailed help

Examples:
  shorin-proton-wrapper --init
  shorin-proton-wrapper /path/to/app.exe
  shorin-proton-wrapper --runner /path/to/proton /path/to/app.exe
  shorin-proton-wrapper -p /path/to/prefix /path/to/app.exe
  shorin-proton-wrapper --install dw
  shorin-proton-wrapper --install-steam dw
  shorin-proton-wrapper --download ge --version GE-Proton10-34
  shorin-proton-wrapper --list-version dw
  shorin-proton-wrapper --update
EOF
}

usage_zh() {
  cat <<'EOF'
用法:
  shorin-proton-wrapper [选项] app.exe [参数...]

选项:
  --init                  初始化，默认路径 ~/.proton
  -p, --prefix 目录       指定前置环境路径，若不存在则初始化
  --status                显示前置环境信息
  --list-runner           列出已安装的运行器
  --runner 路径|ge|dw     指定运行器
  --install ge|dw         安装运行器，默认安装至 ~/.local/share/proton/runners
  --install-steam ge|dw   给 Steam 安装运行器
  --install-lutris ge|dw  给 Lutris 安装运行器
  --download ge|dw        仅下载运行器，默认下载路径为 XDG 下载目录
  --list-version ge|dw    列出可使用的运行器版本
  --version 版本          指定特定版本，默认 latest
  --update                升级所有已安装的 Latest 运行器
  --remove ge|dw          移除默认位置中已安装的运行器
  --remove-steam [ge|dw]  移除 Steam 中指定或全部由 wrapper 安装的运行器
  --remove-lutris [ge|dw] 移除 Lutris 中指定或全部由 wrapper 安装的运行器
  --help-full             显示完整说明

示例:
  shorin-proton-wrapper --init
  shorin-proton-wrapper /path/to/app.exe
  shorin-proton-wrapper --runner /path/to/proton /path/to/app.exe
  shorin-proton-wrapper -p /path/to/prefix /path/to/app.exe
  shorin-proton-wrapper --install dw
  shorin-proton-wrapper --install-steam dw
  shorin-proton-wrapper --download ge --version GE-Proton10-34
  shorin-proton-wrapper --list-version dw
  shorin-proton-wrapper --update
EOF
}

usage_full_en() {
  cat <<'EOF'
Concepts:
  prefix   Compatibility data directory (Wine pfx at prefix/pfx)
  runner   Proton or Wine runtime for Windows executables

Run:
  shorin-proton-wrapper app.exe [args...]
  shorin-proton-wrapper -p ~/Games/game-prefix app.exe
  shorin-proton-wrapper --runner /path/to/proton app.exe

Prefix tools:
  shorin-proton-wrapper --init [-p DIR] [--runner PATH]
  shorin-proton-wrapper --winecfg [-p DIR] [--runner PATH]
  shorin-proton-wrapper --status [-p DIR] [--runner PATH]

Runner discovery:
  shorin-proton-wrapper --list-runner
  shorin-proton-wrapper --list-version ge|dw

Download only:
  shorin-proton-wrapper --download ge [-d DIR] [--version TAG]
  shorin-proton-wrapper --download dw [-d DIR] [--version TAG]

Install runner:
  shorin-proton-wrapper --install ge [-d DIR] [--version TAG]
  shorin-proton-wrapper --install dw [-d DIR] [--version TAG]
  shorin-proton-wrapper --install-steam ge|dw
  shorin-proton-wrapper --install-lutris ge|dw

Update runners:
  shorin-proton-wrapper --update

Remove runner:
  shorin-proton-wrapper --remove ge|dw
  shorin-proton-wrapper --remove-steam [ge|dw]
  shorin-proton-wrapper --remove-lutris [ge|dw]

Install targets:
  local   ~/.local/share/proton/runners
  steam   ~/.local/share/Steam/compatibilitytools.d
  lutris  ~/.local/share/lutris/runners/wine

Environment:
  PROTON_PREFIX     prefix path
  PROTON_RUNNER     runner path
  PROTON_LANG       zh|en
EOF
}

usage_full_zh() {
  cat <<'EOF'
概念:
  prefix   前置环境目录 (Wine 环境在 prefix/pfx)
  runner   运行器 (Proton 或 Wine)

运行:
  shorin-proton-wrapper app.exe [参数...]
  shorin-proton-wrapper -p ~/Games/game-prefix app.exe
  shorin-proton-wrapper --runner /path/to/proton app.exe

前置环境:
  shorin-proton-wrapper --init [-p 目录] [--runner 路径]
  shorin-proton-wrapper --winecfg [-p 目录] [--runner 路径]
  shorin-proton-wrapper --status [-p 目录] [--runner 路径]

运行器发现:
  shorin-proton-wrapper --list-runner
  shorin-proton-wrapper --list-version ge|dw

只下载:
  shorin-proton-wrapper --download ge [-d 目录] [--version 标签]
  shorin-proton-wrapper --download dw [-d 目录] [--version 标签]

安装运行器:
  shorin-proton-wrapper --install ge [-d 目录] [--version 标签]
  shorin-proton-wrapper --install dw [-d 目录] [--version 标签]
  shorin-proton-wrapper --install-steam ge|dw
  shorin-proton-wrapper --install-lutris ge|dw

更新运行器:
  shorin-proton-wrapper --update

移除运行器:
  shorin-proton-wrapper --remove ge|dw
  shorin-proton-wrapper --remove-steam [ge|dw]
  shorin-proton-wrapper --remove-lutris [ge|dw]

安装目标:
  local   ~/.local/share/proton/runners
  steam   ~/.local/share/Steam/compatibilitytools.d
  lutris  ~/.local/share/lutris/runners/wine

环境变量:
  PROTON_PREFIX     前置环境路径
  PROTON_RUNNER     运行器路径
  PROTON_LANG       zh|en
EOF
}

usage() {
  if [[ "$(ui_lang)" == "zh" ]]; then
    usage_zh
  else
    usage_en
  fi
}

usage_full() {
  if [[ "$(ui_lang)" == "zh" ]]; then
    usage_full_zh
  else
    usage_full_en
  fi
}

die() {
  printf 'shorin-proton-wrapper: %s\n' "$*" >&2
  exit 1
}

expand_path() {
  local path="$1"
  case "$path" in
    "~") printf '%s\n' "$HOME" ;;
    "~/"*) printf '%s\n' "$HOME/${path#~/}" ;;
    *) realpath -m "$path" ;;
  esac
}

xdg_download_dir() {
  local value line config
  if command -v xdg-user-dir >/dev/null 2>&1; then
    value="$(xdg-user-dir DOWNLOAD 2>/dev/null || true)"
    if [[ -n "$value" ]]; then
      expand_path "$value"
      return 0
    fi
  fi

  config="${XDG_CONFIG_HOME:-$HOME/.config}/user-dirs.dirs"
  if [[ -r "$config" ]]; then
    while IFS= read -r line; do
      if [[ "$line" =~ ^XDG_DOWNLOAD_DIR=\"(.*)\"$ ]]; then
        value="${BASH_REMATCH[1]}"
        value="${value//\$HOME/$HOME}"
        expand_path "$value"
        return 0
      fi
    done < "$config"
  fi

  printf '%s\n' "$HOME/Downloads"
}

steam_root_dir() {
  if [[ -d "$STEAM_ROOT" ]]; then
    expand_path "$STEAM_ROOT"
  elif [[ -d "$HOME/.local/share/Steam" ]]; then
    printf '%s\n' "$HOME/.local/share/Steam"
  else
    expand_path "$STEAM_ROOT"
  fi
}

default_install_dir() {
  case "$INSTALL_TO" in
    local)
      printf '%s\n' "$LOCAL_RUNNER_DIR"
      ;;
    steam)
      printf '%s\n' "$HOME/.local/share/Steam/compatibilitytools.d"
      ;;
    lutris)
      printf '%s\n' "$HOME/.local/share/lutris/runners/wine"
      ;;
    *)
      die "unknown install target: $INSTALL_TO"
      ;;
  esac
}

runner_name_from_path() {
  local type="$1" path="$2" root
  if [[ "$type" == "proton" ]]; then
    basename "$(dirname "$path")"
    return 0
  fi

  root="$(dirname "$path")"
  if [[ "$(basename "$root")" == "bin" ]]; then
    root="$(dirname "$root")"
  fi
  if [[ "$(basename "$root")" == "files" ]]; then
    root="$(dirname "$root")"
  fi
  basename "$root"
}

wine_root_has_proton() {
  local path="$1" root
  root="$(dirname "$path")"
  if [[ "$(basename "$root")" == "bin" ]]; then
    root="$(dirname "$root")"
  fi
  if [[ "$(basename "$root")" == "files" ]]; then
    root="$(dirname "$root")"
  fi
  [[ -x "$root/proton" ]]
}

reset_runners() {
  RUNNER_TYPES=()
  RUNNER_SOURCES=()
  RUNNER_NAMES=()
  RUNNER_PATHS=()
  RUNNER_SEEN=()
}

add_runner() {
  local type="$1" source="$2" path="$3" name real
  [[ -x "$path" ]] || return 0

  real="$(realpath -m "$path")"
  [[ -n "${RUNNER_SEEN[$real]:-}" ]] && return 0
  RUNNER_SEEN["$real"]=1

  name="$(runner_name_from_path "$type" "$path")"
  RUNNER_TYPES+=("$type")
  RUNNER_SOURCES+=("$source")
  RUNNER_NAMES+=("$name")
  RUNNER_PATHS+=("$real")
}

add_proton_paths() {
  local source="$1" path
  shift || true
  for path in "$@"; do
    add_runner proton "$source" "$path"
  done
}

add_wine_paths() {
  local source="$1" path
  shift || true
  for path in "$@"; do
    if ! wine_root_has_proton "$path"; then
      add_runner wine "$source" "$path"
    fi
  done
}

read_steam_libraries() {
  local file line lib
  for file in \
    "$HOME/.local/share/Steam/steamapps/libraryfolders.vdf" \
    "$HOME/.steam/root/steamapps/libraryfolders.vdf"; do
    [[ -r "$file" ]] || continue
    while IFS= read -r line; do
      if [[ "$line" =~ \"path\"[[:space:]]+\"([^\"]+)\" ]]; then
        lib="${BASH_REMATCH[1]}"
        expand_path "$lib"
      fi
    done < "$file"
  done
}

collect_runners() {
  local lib
  reset_runners

  if [[ -n "${PROTON_RUNNER:-}" ]]; then
    try_resolve_runner_arg "$PROTON_RUNNER" >/dev/null 2>&1 || true
    if [[ -n "${RESOLVED_RUNNER_PATH:-}" ]]; then
      add_runner "$RESOLVED_RUNNER_TYPE" env "$RESOLVED_RUNNER_PATH"
    fi
  fi

  if [[ -n "${PROTON_GE_PATH:-}" ]]; then
    try_resolve_runner_arg "$PROTON_GE_PATH" >/dev/null 2>&1 || true
    if [[ -n "${RESOLVED_RUNNER_PATH:-}" ]]; then
      add_runner "$RESOLVED_RUNNER_TYPE" env "$RESOLVED_RUNNER_PATH"
    fi
  fi

  shopt -s nullglob
  add_proton_paths local "$LOCAL_RUNNER_DIR"/*/proton

  add_proton_paths steam-compat \
    "$HOME/.local/share/Steam/compatibilitytools.d/DW-Proton Latest/proton" \
    "$HOME/.steam/root/compatibilitytools.d/DW-Proton Latest/proton" \
    "$HOME/.local/share/Steam/compatibilitytools.d/Proton-GE Latest/proton" \
    "$HOME/.steam/root/compatibilitytools.d/Proton-GE Latest/proton"
  add_proton_paths steam-compat \
    "$HOME/.local/share/Steam/compatibilitytools.d"/*/proton \
    "$HOME/.steam/root/compatibilitytools.d"/*/proton

  add_proton_paths lutris \
    "$HOME/.local/share/lutris/runners/proton"/*/proton \
    "$HOME/.local/share/lutris/runners/wine"/*/proton \
    "$HOME/.var/app/net.lutris.Lutris/data/lutris/runners/proton"/*/proton \
    "$HOME/.var/app/net.lutris.Lutris/data/lutris/runners/wine"/*/proton

  add_proton_paths steam-official \
    "$HOME/.local/share/Steam/steamapps/common/Proton - Experimental/proton" \
    "$HOME/.steam/root/steamapps/common/Proton - Experimental/proton"
  add_proton_paths steam-official \
    "$HOME/.local/share/Steam/steamapps/common"/Proton*/proton \
    "$HOME/.steam/root/steamapps/common"/Proton*/proton

  while IFS= read -r lib; do
    [[ -n "$lib" ]] || continue
    add_proton_paths steam-library "$lib/steamapps/common"/Proton*/proton
  done < <(read_steam_libraries)

  add_wine_paths lutris-wine \
    "$HOME/.local/share/lutris/runners/wine"/*/files/bin/wine \
    "$HOME/.local/share/lutris/runners/wine"/*/bin/wine \
    "$HOME/.var/app/net.lutris.Lutris/data/lutris/runners/wine"/*/files/bin/wine \
    "$HOME/.var/app/net.lutris.Lutris/data/lutris/runners/wine"/*/bin/wine
  shopt -u nullglob
}

RESOLVED_RUNNER_TYPE=""
RESOLVED_RUNNER_PATH=""

try_resolve_runner_arg() {
  local input path base root product
  input="$1"
  RESOLVED_RUNNER_TYPE=""
  RESOLVED_RUNNER_PATH=""

  if product="$(runner_product_alias "$input")"; then
    try_resolve_runner_product "$product"
    return $?
  fi

  path="$(expand_path "$input")"

  if [[ -d "$path" && -x "$path/proton" ]]; then
    RESOLVED_RUNNER_TYPE="proton"
    RESOLVED_RUNNER_PATH="$(realpath -m "$path/proton")"
    return 0
  fi

  if [[ -f "$path" && -x "$path" ]]; then
    base="$(basename "$path")"
    if [[ "$base" == "proton" ]]; then
      RESOLVED_RUNNER_TYPE="proton"
      RESOLVED_RUNNER_PATH="$(realpath -m "$path")"
      return 0
    fi
    if [[ "$base" == "wine" || "$base" == "wine64" ]]; then
      RESOLVED_RUNNER_TYPE="wine"
      RESOLVED_RUNNER_PATH="$(realpath -m "$path")"
      return 0
    fi
  fi

  if [[ -d "$path" ]]; then
    for root in "$path/files/bin/wine" "$path/bin/wine"; do
      if [[ -x "$root" ]]; then
        RESOLVED_RUNNER_TYPE="wine"
        RESOLVED_RUNNER_PATH="$(realpath -m "$root")"
        return 0
      fi
    done
  fi

  return 1
}

try_resolve_runner_from_prefix() {
  local config_line runner_root
  RESOLVED_RUNNER_TYPE=""
  RESOLVED_RUNNER_PATH=""

  [[ -r "$PREFIX/config_info" ]] || return 1
  config_line="$(sed -n '2p' "$PREFIX/config_info")"
  [[ -n "$config_line" ]] || return 1

  runner_root="$(dirname "$(dirname "$(dirname "$config_line")")")"
  if [[ -x "$runner_root/proton" ]]; then
    RESOLVED_RUNNER_TYPE="proton"
    RESOLVED_RUNNER_PATH="$(realpath -m "$runner_root/proton")"
    return 0
  fi
  if [[ -x "$runner_root/bin/wine" ]]; then
    RESOLVED_RUNNER_TYPE="wine"
    RESOLVED_RUNNER_PATH="$(realpath -m "$runner_root/bin/wine")"
    return 0
  fi

  return 1
}

resolve_runner_arg() {
  try_resolve_runner_arg "$1" || die "runner not found or unsupported: $1"
}

try_select_runner() {
  if [[ -n "$RUNNER_ARG" ]]; then
    try_resolve_runner_arg "$RUNNER_ARG"
    return $?
  fi

  if try_resolve_runner_from_prefix; then
    return 0
  fi

  collect_runners
  if [[ ${#RUNNER_PATHS[@]} -eq 0 ]]; then
    return 1
  fi

  RESOLVED_RUNNER_TYPE="${RUNNER_TYPES[0]}"
  RESOLVED_RUNNER_PATH="${RUNNER_PATHS[0]}"
  return 0
}

select_runner() {
  try_select_runner || die "no Proton or Wine runner found. Use --runner PATH, or install one with shorin-proton-wrapper --install dw"
}

list_runners() {
  local selected_path mark i
  collect_runners

  selected_path=""
  if [[ -n "$RUNNER_ARG" ]]; then
    resolve_runner_arg "$RUNNER_ARG"
    selected_path="$(realpath -m "$RESOLVED_RUNNER_PATH")"
  elif [[ ${#RUNNER_PATHS[@]} -gt 0 ]]; then
    selected_path="${RUNNER_PATHS[0]}"
  fi

  if [[ ${#RUNNER_PATHS[@]} -eq 0 ]]; then
    printf 'No runners found.\n'
    return 0
  fi

  for i in "${!RUNNER_PATHS[@]}"; do
    if [[ "${RUNNER_PATHS[$i]}" == "$selected_path" ]]; then
      printf '  * %s\n' "${RUNNER_NAMES[$i]}"
    else
      printf '    %s\n' "${RUNNER_NAMES[$i]}"
    fi
    printf '      type:   %s\n' "${RUNNER_TYPES[$i]}"
    printf '      source: %s\n' "${RUNNER_SOURCES[$i]}"
    printf '      path:   %s\n' "${RUNNER_PATHS[$i]}"
  done
}

normalize_product() {
  local product="${1,,}"
  case "$product" in
    ge|ge-proton|proton-ge)
      printf 'ge\n'
      ;;
    dw|dwproton|dw-proton|dw_proton)
      printf 'dw\n'
      ;;
    *)
      die "unknown runner product: $1"
      ;;
  esac
}

runner_dir_name() {
  local product="$1"
  case "$product" in
    ge) printf 'Proton-GE Latest' ;;
    dw) printf 'DW-Proton Latest' ;;
    *) die "unknown runner product: $product" ;;
  esac
}

runner_target_dir_name() {
  local product="$1" version="$2" tag="${3:-}"
  if [[ "$version" == "latest" ]]; then
    runner_dir_name "$product"
  else
    printf '%s\n' "${tag:-$version}"
  fi
}

runner_product_alias() {
  local product="${1,,}"
  case "$product" in
    ge|ge-proton|proton-ge)
      printf 'ge\n'
      ;;
    dw|dwproton|dw-proton|dw_proton)
      printf 'dw\n'
      ;;
    *)
      return 1
      ;;
  esac
}

try_resolve_runner_product() {
  local product="$1" name path
  name="$(runner_dir_name "$product")"

  for path in \
    "$LOCAL_RUNNER_DIR/$name/proton" \
    "$HOME/.local/share/Steam/compatibilitytools.d/$name/proton" \
    "$HOME/.steam/root/compatibilitytools.d/$name/proton" \
    "$HOME/.local/share/lutris/runners/proton/$name/proton" \
    "$HOME/.local/share/lutris/runners/wine/$name/proton" \
    "$HOME/.var/app/net.lutris.Lutris/data/lutris/runners/proton/$name/proton" \
    "$HOME/.var/app/net.lutris.Lutris/data/lutris/runners/wine/$name/proton"; do
    if [[ -x "$path" ]]; then
      RESOLVED_RUNNER_TYPE="proton"
      RESOLVED_RUNNER_PATH="$(realpath -m "$path")"
      return 0
    fi
  done

  return 1
}

list_versions() {
  local product="$1"
  product="$(normalize_product "$product")"
  python3 - "$product" <<'PY'
import json
import sys
import urllib.request
import urllib.error

product = sys.argv[1]

if product == "ge":
    url = "https://api.github.com/repos/GloriousEggroll/proton-ge-custom/releases?per_page=20"
elif product == "dw":
    url = "https://dawn.wine/api/v1/repos/dawn-winery/dwproton/releases?limit=20"
else:
    raise SystemExit(f"unknown product: {product}")

request = urllib.request.Request(url, headers={"User-Agent": "proton-cli"})
try:
    with urllib.request.urlopen(request, timeout=30) as response:
        data = json.load(response)
except urllib.error.HTTPError as exc:
    raise SystemExit(f"failed to fetch version list: HTTP {exc.code}")
except urllib.error.URLError as exc:
    raise SystemExit(f"failed to fetch version list: {exc.reason}")

if isinstance(data, dict):
    data = [data]

for release in data:
    tag = release.get("tag_name") or release.get("name") or ""
    if not tag:
        continue
    published = release.get("published_at") or release.get("created_at") or ""
    published = published[:10]
    print(f"{tag}  {published}")
PY
}

release_info() {
  local product="$1" version="$2"
  python3 - "$product" "$version" <<'PY'
import json
import sys
import urllib.request
import urllib.error

product = sys.argv[1]
version = sys.argv[2]

if product == "ge":
    if version == "latest":
        url = "https://api.github.com/repos/GloriousEggroll/proton-ge-custom/releases/latest"
    else:
        url = f"https://api.github.com/repos/GloriousEggroll/proton-ge-custom/releases/tags/{version}"
    archive_suffixes = (".tar.gz",)
    checksum_suffix = ".sha512sum"
elif product == "dw":
    if version == "latest":
        url = "https://dawn.wine/api/v1/repos/dawn-winery/dwproton/releases/latest"
    else:
        url = f"https://dawn.wine/api/v1/repos/dawn-winery/dwproton/releases/tags/{version}"
    archive_suffixes = ("x86_64.tar.xz",)
    checksum_suffix = "x86_64.sha512sum"
else:
    raise SystemExit(f"unknown product: {product}")

request = urllib.request.Request(url, headers={"User-Agent": "proton-cli"})
try:
    with urllib.request.urlopen(request, timeout=30) as response:
        data = json.load(response)
except urllib.error.HTTPError as exc:
    raise SystemExit(f"release not found for {product} {version}: HTTP {exc.code}")
except urllib.error.URLError as exc:
    raise SystemExit(f"failed to fetch release info for {product} {version}: {exc.reason}")

assets = data.get("assets") or []
tag = data.get("tag_name") or data.get("name") or version
archive = None
checksum = None

for asset in assets:
    name = asset.get("name", "")
    download = asset.get("browser_download_url", "")
    if not download:
        continue
    if archive is None and any(name.endswith(suffix) for suffix in archive_suffixes):
        archive = (name, download)
    if checksum is None and name.endswith(checksum_suffix):
        checksum = (name, download)

if archive is None:
    raise SystemExit(f"archive asset not found for {product} {version}")
if checksum is None:
    raise SystemExit(f"checksum asset not found for {product} {version}")

print(tag)
print(archive[0])
print(archive[1])
print(checksum[0])
print(checksum[1])
PY
}

fetch_file() {
  local url="$1" dest="$2" tmp
  if [[ -s "$dest" ]]; then
    printf 'Using existing: %s\n' "$dest"
    return 0
  fi

  tmp="$dest.part"
  rm -f "$tmp"
  if command -v curl >/dev/null 2>&1; then
    curl -L --fail --progress-bar -o "$tmp" "$url"
  elif command -v wget >/dev/null 2>&1; then
    wget -O "$tmp" "$url"
  else
    die "curl or wget is required for downloads"
  fi
  mv "$tmp" "$dest"
}

verify_archive() {
  local dir="$1" checksum_name="$2"
  (cd "$dir" && sha512sum -c "$checksum_name")
}

find_extracted_runner_root() {
  local extract_dir="$1" path
  if [[ -x "$extract_dir/proton" ]]; then
    printf '%s\n' "$extract_dir"
    return 0
  fi

  shopt -s nullglob
  for path in "$extract_dir"/*; do
    if [[ -d "$path" && -x "$path/proton" ]]; then
      shopt -u nullglob
      printf '%s\n' "$path"
      return 0
    fi
  done
  shopt -u nullglob

  return 1
}

installed_runner_version() {
  local target="$1" product="$2" line version

  if [[ -r "$target/.shorin-proton-wrapper-version" ]]; then
    sed -n '1p' "$target/.shorin-proton-wrapper-version"
    return 0
  fi

  if [[ -r "$target/version" ]]; then
    line="$(sed -n '1p' "$target/version")"
    case "$product" in
      ge)
        version="$(printf '%s\n' "$line" | grep -o 'GE-Proton[^[:space:]]*' | head -n 1 || true)"
        ;;
      dw)
        version="$(printf '%s\n' "$line" | grep -o 'dwproton-[^[:space:]]*' | head -n 1 || true)"
        ;;
      *)
        version=""
        ;;
    esac
    [[ -n "$version" ]] && printf '%s\n' "$version"
  fi
}

download_runner() {
  local product info tag archive_name archive_url checksum_name checksum_url dir release_output
  product="$(normalize_product "$PRODUCT")"
  info=()
  release_output="$(release_info "$product" "$VERSION" 2>&1)" || die "$release_output"
  mapfile -t info <<< "$release_output"
  tag="${info[0]:-}" archive_name="${info[1]:-}" archive_url="${info[2]:-}" checksum_name="${info[3]:-}" checksum_url="${info[4]:-}"
  [[ -n "$tag" ]] || die "release not found for $product $VERSION"
  dir="$(expand_path "${DOWNLOAD_DIR:-$(xdg_download_dir)}")"

  mkdir -p "$dir"
  fetch_file "$archive_url" "$dir/$archive_name"
  fetch_file "$checksum_url" "$dir/$checksum_name"
  verify_archive "$dir" "$checksum_name"

  printf 'Downloaded: %s\n' "$dir/$archive_name"
  printf 'Checksum:   %s\n' "$dir/$checksum_name"
  printf 'Verified:   yes\n'
  printf 'Version:    %s\n' "$tag"
}

install_runner() {
  local product info tag archive_name archive_url checksum_name checksum_url download_dir install_dir work_dir use_existing extract_dir root target temp_download_dir dir_name release_output
  product="$(normalize_product "$PRODUCT")"
  info=()
  release_output="$(release_info "$product" "$VERSION" 2>&1)" || die "$release_output"
  mapfile -t info <<< "$release_output"
  tag="${info[0]:-}" archive_name="${info[1]:-}" archive_url="${info[2]:-}" checksum_name="${info[3]:-}" checksum_url="${info[4]:-}"
  [[ -n "$tag" ]] || die "release not found for $product $VERSION"
  download_dir="$(expand_path "${DOWNLOAD_DIR:-$(xdg_download_dir)}")"
  install_dir="$(expand_path "${INSTALL_DIR:-$(default_install_dir)}")"
  dir_name="$(runner_target_dir_name "$product" "$VERSION" "$tag")"
  target="$install_dir/$dir_name"

  mkdir -p "$download_dir" "$install_dir"

  use_existing=0
  if [[ -s "$download_dir/$archive_name" && -s "$download_dir/$checksum_name" ]]; then
    work_dir="$download_dir"
    use_existing=1
    printf 'Using downloaded files in: %s\n' "$download_dir"
  else
    temp_download_dir="$(mktemp -d "$download_dir/.proton-install.XXXXXX")"
    work_dir="$temp_download_dir"
    fetch_file "$archive_url" "$work_dir/$archive_name"
    fetch_file "$checksum_url" "$work_dir/$checksum_name"
  fi

  verify_archive "$work_dir" "$checksum_name"

  extract_dir="$(mktemp -d)"
  tar -xf "$work_dir/$archive_name" -C "$extract_dir"
  root="$(find_extracted_runner_root "$extract_dir")" || die "extracted archive does not contain a proton runner"
  if [[ -e "$target" && ! -x "$target/proton" ]]; then
    rm -rf "$extract_dir"
    die "install target exists but is not a runner: $target"
  fi
  rm -rf "$target"
  mv "$root" "$target"
  printf '%s\n' "$tag" > "$target/.shorin-proton-wrapper-version"
  rm -rf "$extract_dir"

  if [[ $use_existing -eq 0 ]]; then
    rm -rf "$work_dir"
  elif [[ $CLEAN -eq 1 ]]; then
    rm -f "$download_dir/$archive_name" "$download_dir/$checksum_name"
  fi

  printf 'Installed: %s\n' "$target/proton"
}

update_runner() {
  local product target install_to_old old_product old_version release_output info tag current found updated skipped
  install_to_old="$INSTALL_TO"
  old_product="$PRODUCT"
  old_version="$VERSION"
  VERSION="latest"
  info=()
  found=0
  updated=0
  skipped=0

  for INSTALL_TO in local steam lutris; do
    for product in ge dw; do
      PRODUCT="$product"
      target="$(expand_path "$(default_install_dir)")/$(runner_dir_name "$product")"
      if [[ -e "$target" ]]; then
        found=1
        release_output="$(release_info "$product" latest 2>&1)" || die "$release_output"
        mapfile -t info <<< "$release_output"
        tag="${info[0]:-}"
        [[ -n "$tag" ]] || die "release not found for $product latest"
        current="$(installed_runner_version "$target" "$product" || true)"
        if [[ "$current" == "$tag" ]]; then
          printf 'Already latest: %s (%s)\n' "$target" "$tag"
          skipped=1
        else
          printf 'Updating: %s\n' "$target"
          if [[ -n "$current" ]]; then
            printf '  %s -> %s\n' "$current" "$tag"
          fi
          install_runner
          updated=1
        fi
      fi
    done
  done

  INSTALL_TO="$install_to_old"
  PRODUCT="$old_product"
  VERSION="$old_version"
  [[ $found -eq 1 ]] || die "no installed Latest runners found"
  if [[ $updated -eq 0 && $skipped -eq 1 ]]; then
    printf 'All installed Latest runners are up to date.\n'
  fi
}

remove_runner() {
  local product dir_name install_dir target lang
  product="$(normalize_product "$PRODUCT")"
  dir_name="$(runner_target_dir_name "$product" "$VERSION")"
  install_dir="$(expand_path "${INSTALL_DIR:-$(default_install_dir)}")"
  target="$install_dir/$dir_name"
  lang="$(ui_lang)"

  if [[ ! -e "$target" ]]; then
    if [[ "$lang" == "zh" ]]; then
      die "$dir_name 未安装"
    else
      die "$dir_name is not installed"
    fi
  fi

  rm -rf "$target"

  if [[ "$lang" == "zh" ]]; then
    printf '已移除: %s\n' "$target"
  else
    printf 'Removed: %s\n' "$target"
  fi
}

remove_all_runners() {
  local install_dir path removed lang
  install_dir="$(expand_path "${INSTALL_DIR:-$(default_install_dir)}")"
  lang="$(ui_lang)"
  removed=0

  shopt -s nullglob
  for path in "$install_dir"/*; do
    if [[ -d "$path" && -f "$path/.shorin-proton-wrapper-version" ]]; then
      rm -rf "$path"
      removed=1
      if [[ "$lang" == "zh" ]]; then
        printf '已移除: %s\n' "$path"
      else
        printf 'Removed: %s\n' "$path"
      fi
    fi
  done
  shopt -u nullglob

  if [[ $removed -eq 0 ]]; then
    if [[ "$lang" == "zh" ]]; then
      die "没有找到由 shorin-proton-wrapper 安装的运行器: $install_dir"
    else
      die "no runners installed by shorin-proton-wrapper found in: $install_dir"
    fi
  fi
}

ensure_runner_product() {
  local product="$1" old_product old_version old_install_to
  if try_resolve_runner_product "$product"; then
    return 0
  fi

  old_product="$PRODUCT"
  old_version="$VERSION"
  old_install_to="$INSTALL_TO"

  PRODUCT="$product"
  VERSION="latest"
  INSTALL_TO="local"
  install_runner

  PRODUCT="$old_product"
  VERSION="$old_version"
  INSTALL_TO="$old_install_to"

  try_resolve_runner_product "$product"
}

select_init_runner() {
  local product
  if [[ -n "$RUNNER_ARG" ]]; then
    if product="$(runner_product_alias "$RUNNER_ARG")"; then
      ensure_runner_product "$product" || die "failed to install runner: $product"
    else
      resolve_runner_arg "$RUNNER_ARG"
    fi
    return 0
  fi

  ensure_runner_product dw || die "failed to install runner: dw"
}

run_proton_command() {
  local runner="$1"
  shift
  mkdir -p "$PREFIX"
  env \
    STEAM_COMPAT_DATA_PATH="$PREFIX" \
    STEAM_COMPAT_CLIENT_INSTALL_PATH="$(steam_root_dir)" \
    SteamAppId="${SteamAppId:-0}" \
    SteamGameId="${SteamGameId:-0}" \
    "$runner" "$@"
}

exec_proton_command() {
  local runner="$1"
  shift
  mkdir -p "$PREFIX"
  exec env \
    STEAM_COMPAT_DATA_PATH="$PREFIX" \
    STEAM_COMPAT_CLIENT_INSTALL_PATH="$(steam_root_dir)" \
    SteamAppId="${SteamAppId:-0}" \
    SteamGameId="${SteamGameId:-0}" \
    "$runner" "$@"
}

run_wine_command() {
  local runner="$1"
  shift
  mkdir -p "$PREFIX/pfx"
  env WINEPREFIX="$PREFIX/pfx" "$runner" "$@"
}

exec_wine_command() {
  local runner="$1"
  shift
  mkdir -p "$PREFIX/pfx"
  exec env WINEPREFIX="$PREFIX/pfx" "$runner" "$@"
}

init_prefix() {
  select_init_runner
  if [[ "$RESOLVED_RUNNER_TYPE" == "proton" ]]; then
    run_proton_command "$RESOLVED_RUNNER_PATH" run wineboot -u
  else
    run_wine_command "$RESOLVED_RUNNER_PATH" wineboot -u
  fi
}

ensure_initialized() {
  if [[ ! -d "$PREFIX/pfx" ]]; then
    init_prefix
  fi
}

open_winecfg() {
  select_runner
  if [[ "$RESOLVED_RUNNER_TYPE" == "proton" ]]; then
    exec_proton_command "$RESOLVED_RUNNER_PATH" run winecfg
  else
    exec_wine_command "$RESOLVED_RUNNER_PATH" winecfg
  fi
}

run_target() {
  local workdir exe
  select_runner
  ensure_initialized

  TARGET="$(expand_path "$TARGET")"
  [[ -e "$TARGET" ]] || die "target not found: $TARGET"
  workdir="$(dirname "$TARGET")"
  exe="./$(basename "$TARGET")"

  cd "$workdir"
  if [[ "$RESOLVED_RUNNER_TYPE" == "proton" ]]; then
    exec_proton_command "$RESOLVED_RUNNER_PATH" waitforexitandrun "$exe" "${TARGET_ARGS[@]}"
  else
    exec_wine_command "$RESOLVED_RUNNER_PATH" "$exe" "${TARGET_ARGS[@]}"
  fi
}

show_status() {
  local runner_type runner_path initialized install_dir download_dir lang
  PREFIX="$(expand_path "$PREFIX")"
  download_dir="$(expand_path "${DOWNLOAD_DIR:-$(xdg_download_dir)}")"
  install_dir="$(expand_path "${INSTALL_DIR:-$(default_install_dir)}")"
  lang="$(ui_lang)"

  runner_type="not found"
  runner_path="not found"
  if try_select_runner >/dev/null 2>&1; then
    runner_type="$RESOLVED_RUNNER_TYPE"
    runner_path="$RESOLVED_RUNNER_PATH"
  fi

  initialized="no"
  if [[ -d "$PREFIX/pfx" ]]; then
    initialized="yes"
  fi

  if [[ "$lang" == "zh" ]]; then
    [[ "$initialized" == "yes" ]] && initialized="是" || initialized="否"
    printf '前置环境:   %s\n' "$PREFIX"
    printf 'PFX:        %s\n' "$PREFIX/pfx"
    printf '已初始化:   %s\n' "$initialized"
    printf '运行器类型: %s\n' "$runner_type"
    printf '运行器:     %s\n' "$runner_path"
    printf '下载目录:   %s\n' "$download_dir"
    printf '安装目录:   %s\n' "$install_dir"
  else
    printf 'Prefix:      %s\n' "$PREFIX"
    printf 'PFX:         %s\n' "$PREFIX/pfx"
    printf 'Initialized: %s\n' "$initialized"
    printf 'Runner type: %s\n' "$runner_type"
    printf 'Runner:      %s\n' "$runner_path"
    printf 'Download:    %s\n' "$download_dir"
    printf 'Install dir: %s\n' "$install_dir"
  fi
}

parse_args() {
  if [[ $# -eq 0 ]]; then
    usage
    exit 0
  fi

  while [[ $# -gt 0 ]]; do
    case "$1" in
      -h|--help)
        usage
        exit 0
        ;;
      --help-full)
        usage_full
        exit 0
        ;;
      --lang)
        [[ $# -ge 2 ]] || die "$1 requires zh or en"
        case "${2,,}" in
          zh|en) LANG_OVERRIDE="${2,,}" ;;
          *) die "unsupported language: $2" ;;
        esac
        shift 2
        ;;
      -p|--prefix)
        [[ $# -ge 2 ]] || die "$1 requires a directory"
        PREFIX="$2"
        shift 2
        ;;
      --runner)
        [[ $# -ge 2 ]] || die "$1 requires a path"
        RUNNER_ARG="$2"
        shift 2
        while [[ $# -gt 0 && ! "$1" =~ ^- ]] && ! [[ -e "$RUNNER_ARG" || "$RUNNER_ARG" == */proton || "$RUNNER_ARG" == */wine || "$RUNNER_ARG" == */wine64 ]]; do
          RUNNER_ARG="$RUNNER_ARG $1"
          shift
        done
        ;;
      -d|--directory)
        [[ $# -ge 2 ]] || die "$1 requires a directory"
        DOWNLOAD_DIR="$2"
        shift 2
        ;;
      --version)
        [[ $# -ge 2 ]] || die "$1 requires a version"
        VERSION="$2"
        shift 2
        ;;
      --clean)
        CLEAN=1
        shift
        ;;
      --list-runner)
        ACTION="list"
        shift
        ;;
      --list-version)
        [[ $# -ge 2 ]] || die "$1 requires ge or dw"
        ACTION="list-version"
        PRODUCT="$2"
        shift 2
        ;;
      --status)
        ACTION="status"
        shift
        ;;
      --init)
        ACTION="init"
        shift
        ;;
      --winecfg)
        ACTION="winecfg"
        shift
        ;;
      --download)
        [[ $# -ge 2 ]] || die "$1 requires ge or dw"
        ACTION="download"
        PRODUCT="$2"
        shift 2
        ;;
      --install)
        [[ $# -ge 2 ]] || die "$1 requires ge or dw"
        ACTION="install"
        PRODUCT="$2"
        shift 2
        ;;
      --install-steam)
        [[ $# -ge 2 ]] || die "$1 requires ge or dw"
        ACTION="install"
        PRODUCT="$2"
        INSTALL_TO="steam"
        shift 2
        ;;
      --install-lutris)
        [[ $# -ge 2 ]] || die "$1 requires ge or dw"
        ACTION="install"
        PRODUCT="$2"
        INSTALL_TO="lutris"
        shift 2
        ;;
      --update)
        ACTION="update"
        shift
        ;;
      --remove)
        [[ $# -ge 2 ]] || die "$1 requires ge or dw"
        ACTION="remove"
        PRODUCT="$2"
        shift 2
        ;;
      --remove-steam)
        INSTALL_TO="steam"
        if [[ $# -ge 2 && ! "$2" =~ ^- ]]; then
          ACTION="remove"
          PRODUCT="$2"
          shift 2
        else
          ACTION="remove-all"
          shift
        fi
        ;;
      --remove-lutris)
        INSTALL_TO="lutris"
        if [[ $# -ge 2 && ! "$2" =~ ^- ]]; then
          ACTION="remove"
          PRODUCT="$2"
          shift 2
        else
          ACTION="remove-all"
          shift
        fi
        ;;
      --)
        shift
        [[ $# -ge 1 ]] || die "-- requires a target"
        TARGET="$1"
        shift
        TARGET_ARGS=("$@")
        break
        ;;
      -*)
        die "unknown option: $1"
        ;;
      *)
        if [[ -z "$ACTION" ]]; then
          TARGET="$1"
          shift
          TARGET_ARGS=("$@")
          break
        fi
        die "unexpected argument for $ACTION: $1"
        ;;
    esac
  done
}

parse_args "$@"
PREFIX="$(expand_path "$PREFIX")"

case "$ACTION" in
  list)
    list_runners
    ;;
  list-version)
    list_versions "$PRODUCT"
    ;;
  status)
    show_status
    ;;
  init)
    init_prefix
    ;;
  winecfg)
    open_winecfg
    ;;
  download)
    download_runner
    ;;
  install)
    install_runner
    ;;
  update)
    update_runner
    ;;
  remove)
    remove_runner
    ;;
  remove-all)
    remove_all_runners
    ;;
  "")
    [[ -n "$TARGET" ]] || die "no target provided"
    run_target
    ;;
  *)
    die "unknown action: $ACTION"
    ;;
esac
